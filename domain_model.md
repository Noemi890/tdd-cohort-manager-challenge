```
As a Cohort-Manager, so that I can manage them, I want to be able do add a cohort.

| addCohort(name(@string)) | | return the list with the cohort added |
```

```
As a Cohort-Manager, so that I can view a specific cohort, I want to be able to search by a specific name.

| searchByName(name(@string)) | | return the cohort searched |